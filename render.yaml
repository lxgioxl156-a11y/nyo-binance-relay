services:
  - type: web
    name: nyo-binance-relay
    runtime: python
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn relay:app --host 0.0.0.0 --port $PORT